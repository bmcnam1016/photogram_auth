<div class="container">
    <div class="row">
        <div class="col-md-offset-3 col-md-6">
  <% if @user.id == @like.user_id %>
        <dt><a href="/photos/new" button type="button" class="btn btn-primary btn-lg btn-block"">+</a></dt>
  <% @photos.each do |photo| %>
      <dd><img src="<%= photo.image %>" width="555"></dd>
      <% end %>
      <% end %>
      </div>
    </div>
</div>
